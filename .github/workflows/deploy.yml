name: Docker CI

on:
  push:
    branches:
      - main
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
        run: |
          echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin

      - name: Build and push Docker image
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
        run: |
          docker build -t $DOCKER_USERNAME/go-blog:latest .
          docker push $DOCKER_USERNAME/go-blog:latest

      - name: Upload docker-compose.yml artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-compose-file
          path: golang-simple-blog-app/docker-compose.yml

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download docker-compose.yml artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-compose-file

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          host_key_checking: false # Temporarily disabled - address for production
          script: |
            # Define target directory
            TARGET_DIR="/home/${{ secrets.EC2_USERNAME }}/golang-simple-blog-app"

            # Create target directory if it doesn't exist
            mkdir -p $TARGET_DIR

            # Move downloaded docker-compose.yml to target directory
            # The artifact is downloaded to the runner's workspace root
            mv docker-compose.yml $TARGET_DIR/

            # Change to target directory
            cd $TARGET_DIR

            # Verify docker-compose.yml exists (optional, but good for debugging)
            if [ ! -f docker-compose.yml ]; then
              echo "Error: docker-compose.yml not found in $PWD after move"
              exit 1
            fi

            # Pull the latest image and restart containers
            docker-compose pull app
            docker-compose down
            docker-compose up -d 